{
  "name": "criminal-network-analyser",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"cd frontend && npm run dev\" \"python -m uvicorn backend.main:app --reload --port 8000\"",
    "start": "cd frontend && npm run dev",
    "frontend": "cd frontend && npm run dev",
    "backend": "python -m uvicorn backend.main:app --reload --port 8000"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
